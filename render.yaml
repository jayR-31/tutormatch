services:
  - type: web
    name: tutormatch
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
    disk:
      name: tutor-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
